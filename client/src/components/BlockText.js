import React from 'react';
import '../styles/components/BlockText.scss';

const BlockText = ({ children }) => (
  <div className="BlockText-container">
    {children}
  </div>
);

export default BlockText;
